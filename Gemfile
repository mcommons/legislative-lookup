source :rubygems
gem 'rails', '3.0.8' # or should it be 2.3.16?
gem 'fastercsv', '1.5.5'
gem 'GeoRuby', '1.3.3' # not sure if we need this. it was in vendor/gems but comment out in environment.rb
gem 'spatial_adapter', '1.3.1'
gem 'postgres', '0.7.9.2008.01.28'
gem 'rdoc' # our rake tasks load this
gem 'rspec', '1.3.1'
gem 'rspec-rails', '1.3.3'

group :development do
  gem 'capistrano'
end

group :production do
  gem 'passenger', '3.0.19'
end
